diff '--color=auto' -ru a/daemon/automount.c b/daemon/automount.c
--- a/daemon/automount.c	2023-11-02 20:46:34.000000000 -0400
+++ b/daemon/automount.c	2026-04-13 11:13:29.676864061 -0400
@@ -1,7 +1,7 @@
 /* ----------------------------------------------------------------------- *
  *
  *  automount.c - Linux automounter daemon
- *   
+ *
  *   Copyright 1997 Transmeta Corporation - All Rights Reserved
  *   Copyright 1999-2000 Jeremy Fitzhardinge <jeremy@goop.org>
  *   Copyright 2001-2005 Ian Kent <raven@themaw.net>
@@ -11,7 +11,7 @@
  *   the Free Software Foundation, Inc., 675 Mass Ave, Cambridge MA 02139,
  *   USA; either version 2 of the License, or (at your option) any later
  *   version.
- *   
+ *
  *   This program is distributed in the hope that it will be useful,
  *   but WITHOUT ANY WARRANTY; without even the implied warranty of
  *   MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
@@ -49,6 +49,7 @@
 #endif
 
 #ifndef __SWORD_TYPE
+#ifdef __GLIBC__
 #if __WORDSIZE == 32
 # define __SWORD_TYPE	int
 #elif __WORDSIZE == 64
@@ -56,7 +57,10 @@
 #else
 #error
 #endif
-#endif
+#else
+#define __SWORD_TYPE long int
+#endif // __GLIBC__
+#endif // __SWORD_TYPE
 
 const char *program;		/* Initialized with argv[0] */
 const char *version = VERSION_STRING;	/* Program version */
@@ -300,7 +304,7 @@
 			     dev, buf, st.st_dev);
 			return -1;
 		}
-			     
+
 		/*
 		 * Last element of path may be a symbolic link; all others
 		 * are directories (and the last directory element is
@@ -473,7 +477,7 @@
 
 	counter.count = 0;
 	counter.dev = dev;
-	
+
 	if (walk_tree(path, counter_fn, 1, ap, &counter) == -1)
 		return -1;
 
@@ -1691,7 +1695,7 @@
 	sc->done = 1;
 
 	/*
-	 * Startup condition mutex must be locked during 
+	 * Startup condition mutex must be locked during
 	 * the startup process.
 	 */
 	status = pthread_cond_signal(&sc->cond);
diff '--color=auto' -ru a/include/hash.h b/include/hash.h
--- a/include/hash.h	2023-11-02 20:46:34.000000000 -0400
+++ b/include/hash.h	2026-04-13 11:09:05.713525202 -0400
@@ -17,6 +17,7 @@
  * fs/inode.c.  It's not actually prime any more (the previous primes
  * were actively bad for hashing), but the name remains.
  */
+#ifdef __GLIBC__
 #if __WORDSIZE == 32
 #define GOLDEN_RATIO_PRIME GOLDEN_RATIO_32
 #define hash_long(val, bits) hash_32(val, bits)
@@ -26,6 +27,10 @@
 #else
 #error Wordsize not 32 or 64
 #endif
+#else
+#define hash_long(val, bits) hash_64(val, bits)
+#define GOLDEN_RATIO_PRIME GOLDEN_RATIO_64
+#endif // __GLIBC__
 
 /* String based hash function */
 static uint32_t inline hash(const char *key, unsigned int size)
